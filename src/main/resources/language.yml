language:
  # 变量：${POINTS}为指令所需支付的“点券”
  #       ${NOW}为玩家拥有的“点券”

  #点券不足
  not_enough: "[PaidCommandPoints] > §a§b点券不足，指令售价${POINTS}点券，您的点券：${NOW}"
  #成功购买使用指令
  success: "[PaidCommandPoints] > §b成功使用指令，花费${POINTS}点券，你现有点券：${NOW}"
